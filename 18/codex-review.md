- **Major — [scripts/install_hooks.sh](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/scripts/install_hooks.sh:12), [scripts/uninstall_hooks.sh](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/scripts/uninstall_hooks.sh:11):** Claude hooks are written to `~/.claude/settings.local.json`, which is not a global settings source. User-wide hooks belong in `~/.claude/settings.json`; `settings.local.json` is project-local. The installer can report success while no Claude hook fires. [Claude hook locations](https://code.claude.com/docs/en/hooks). **Fix:** target `~/.claude/settings.json` in both scripts and migrate/remove entries from the stray path.
- **Major — [scripts/install_hooks.sh](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/scripts/install_hooks.sh:117), [codex_hook_client.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/codex_hook_client.py:36):** Codex observation hooks run synchronously; the client waits on blocking `flock()` and an HTTP response for up to three seconds. Thus bridge failure or log contention delays Codex, violating observe-only operation. Simply adding `async:true` is not valid for the specified Codex 0.147.0: that release [skips ordinary async hooks and runs `SessionEnd` synchronously](https://github.com/openai/codex/blob/rust-v0.147.0/codex-rs/hooks/src/engine/discovery.rs#L449-L475). **Fix:** use bounded nonblocking one-way IPC, such as an asyncio-integrated Unix datagram socket, or require an async-capable Codex release; make logging nonblocking and add latency tests with a stalled receiver.
- **Major — [codex_hook_client.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/codex_hook_client.py:63), [hook_client.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/hook_client.py:49):** `urllib.request.urlopen()` honors inherited HTTP proxy settings. Without matching `NO_PROXY`, a loopback request is sent through the proxy with `X-Bridge-Token`, cwd, session identifiers, tool input, and messages intact. This was reproduced with `http_proxy` set. **Fix:** use a proxy-disabled opener, direct loopback `HTTPConnection`, or the local datagram transport; add a proxy-environment regression test.
- **Major — [server/main.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/server/main.py:153):** `sessions` is capped at six, but evicted entries are deliberately retained indefinitely in `session_info`. With missed `SessionEnd` hooks, 1,000 starts produced `sessions=6` and `session_info=1000`; every two-second status request then serializes the growing map. **Fix:** remove victim metadata on eviction or use a hard-capped LRU/TTL cache, with a churn-without-`SessionEnd` test.
- **Major — [server/main.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/server/main.py:211), [tests/test_events.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/tests/test_events.py:376):** An unresolved `/decision` suppresses every non-input transition, including `StopFailure` and `PostToolUseFailure`. Required red/error state remains amber/input, and the test explicitly asserts the incorrect result. **Fix:** suppress only benign transitions; allow error states. `StopFailure` should fail-close same-session approvals, and `/decision` cleanup must not overwrite the terminal error.
- **Major — [server/main.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/server/main.py:465), [codex_hook_client.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/codex_hook_client.py:76):** Observe-only Codex approvals have no tracked correlation. A supported parallel ordering, `PermissionRequest(A) → PostToolUse(B)`, changes the LED from `input` to `thinking` while A still awaits approval. The client also drops `tool_use_id`. **Fix:** maintain a bounded passive approval tracker keyed by the strongest available turn/tool identity, forward `tool_use_id`, and clear only matching or terminal events.
- **Major — [server/main.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/server/main.py:169):** Pending-approval ownership is unsafe across eviction/end/restart. After forced all-busy eviction, `SessionEnd` does not deny the detached future, so it can still be accepted. Separately, an old `/decision` coroutine can overwrite a restarted same-ID session’s LED. The public `pending[].agent_index` also becomes `null`. **Fix:** track allocation generations/ownership separately, deny pending requests by session even when no key is assigned, and preserve the existing numeric status field.
- **Major — [server/main.py](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/server/main.py:249):** Newly routed `PreToolUse`/`PostToolUse` repeatedly reapply unchanged `thinking`. During the animator’s family-color phase this causes an unnecessary state-color write, followed by the animator writing the same color again, defeating LED dedup #9. **Fix:** return early when the requested logical `{state, family}` is unchanged; test with a recording adapter during both animation phases.
- **Major — [scripts/uninstall_hooks.sh](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/scripts/uninstall_hooks.sh:159):** `--restore` rejects the newest backup when it contains an earlier claudemicro hook and falls back to an older “clean” snapshot. After unrelated settings changes and reinstall, restore silently discards those changes. Failed installs can also leave `.absent` markers that later delete a legitimate config. **Fix:** use a transactional installation manifest and restore the newest snapshot after selectively stripping owned hooks.
- **Minor — [console/index.html](/Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/claudeMicro/console/index.html:375):** The bridge token remains in `?token=`, exposing it through browser history and initial request/proxy logs. **Fix:** obtain it from a URL fragment or secure session cookie and immediately scrub the URL.
**Overall verdict: REQUEST-CHANGES**
