"""Thin desktop wrapper for the ClaudeMicro bridge.

GUI dependencies intentionally are not imported from this package so the bridge and
``python -m app --smoke`` keep working in headless environments.
"""

